# mini-agent.config.yaml
# Example configuration file for mini-agent CLI

# Data storage directory (relative to working directory)
DATA_STORAGE_DIR: ".mini-agent"

# Logging configuration
LOGGING:
  STDOUT_LOG_LEVEL: "info"
  # Uncomment to enable file logging (relative to .mini-agent)
  # FILE_LOG_PATH: "logs/mini-agent.log"
  FILE_LOG_LEVEL: "debug"


